# Optional — src/lib/firebase.ts already has fallback defaults for these,
# only needed if you want to point at a different Cloudinary account.
VITE_CLOUDINARY_CLOUD_NAME=vertexel
VITE_CLOUDINARY_UPLOAD_PRESET=vertexel_unsigned
